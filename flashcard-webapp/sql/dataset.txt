("What is an operating system?", "An operating system is software that manages the computer hardware and software resources, providing various services for computer programs.")
("What are the main functions of an operating system?", "Resource allocation, scheduling, interfacing, and managing I/O operations.")
("What is the kernel of an operating system?", "The kernel is the core component of an operating system, managing the system's resources and the communication between hardware and software components.")
("What is a system call?", "A system call is a programmatic way a computer program requests a service from the kernel of the operating system.")
("What is a process in the context of operating systems?", "A process is an instance of a computer program that is being executed. It contains the program code and its current activity.")
("What is a process control block (PCB)", "A process control block is a data structure in the operating system kernel containing the information needed to manage a particular process.")
("What is a thread in the context of operating systems?", "A thread is the smallest sequence of programmed instructions that can be managed independently by a scheduler.")
("What is the difference between a process and a thread?", "A process has a self-contained execution environment with its own memory space, while a thread is a subset of a process. Multiple threads within a process share the same data space with the main thread and can therefore communicate with each other more easily than if they were separate processes.")
("What is CPU scheduling?", "CPU scheduling is the process of determining which process in the ready queue is to be allocated the CPU.")
("What are the common types of CPU scheduling algorithms?", "First-Come, First-Served (FCFS) Shortest-Job-Next (SJN) Round Robin (RR) Priority Scheduling, and Multilevel Queue Scheduling.")
("What is process synchronization in operating systems?", "Process synchronization refers to the handling of the situation where several processes access and manipulate the same data concurrently and the outcome of the execution depends on the particular order in which the access takes place.")
("What is a critical section in operating systems?", "In concurrent programming, a critical section is a piece of code that accesses a shared resource and must not be concurrently executed by more than one thread.")
("What is a deadlock in an operating system?", "A deadlock is a situation where a set of processes are blocked because each process is holding a resource and waiting for another resource acquired by some other process.")
("What are the four Coffman conditions for deadlock?", "Mutual Exclusion, Hold and Wait, No Preemption, and Circular")
("What is the role of main memory in an operating system?", "Main memory, or RAM, is the primary, volatile memory in a computer where the operating system, application programs, and data in current use are kept for quick access by the computer's processor.")
("What is paging in the context of operating systems?", "Paging is a method of memory management that allows physical-address space of a process to be noncontiguous. It avoids the considerable problem of fitting memory chunks of varying sizes onto the backing store.")
("What is virtual memory in an operating system?", "Virtual memory is a memory management technique that provides an 'idealized abstraction of the storage resources that are actually available on a given machine'.")
("What are the benefits of using virtual memory?", "Virtual memory allows separation of user logical memory from physical memory, and simplifies memory management. It also allows execution of programs that may not entirely fit into memory.")
("What is a file system in the context of operating systems?", "A file system is a method for storing and organizing computer files and their data. It organizes the files into a database for the storage, organization, manipulation, and retrieval by the operating system.")
("What are the basic operations of a file system?", "Creating files, reading files, writing to files, deleting files, and navigating through the file system.")
("What is a real-time operating system?", "A real-time operating system (RTOS)is an operating system intended to serve real-time applications that process data as it comes in, typically without buffer delays.")
("What is the role of the command interpreter in an operating system?", "The command interpreter, or shell, reads and interprets commands from the user or from files.")
("What is monolithic architecture in an operating system?", "Monolithic architecture is a traditional design strategy where the entire operating system runs as a single program in kernel mode.")
("What is a microkernel?", "A microkernel is a minimal computer operating system kernel which provides the mechanisms needed to implement an operating system, but does not implement system services itself, leaving those as user space processes.")
("What is the context switch in operating systems?", "A context switch is the process of storing and restoring the state (context)of a process so that execution can be resumed from the same point at a later time.")
("What is Interprocess Communication (IPC)", "Interprocess Communication (IPC)refers to a set of methods for the exchange of data among multiple threads in one or more processes. Processes may be running on one or more computers connected by a network.")
("What are the benefits of multithreaded programming?", "Benefits include increased responsiveness to users, resource sharing within the process, economy (it's cheaper to create and context switch threads) and the ability to take advantage of multiprocessor architectures.")
("What is the difference between user-level threads and kernel-level threads?", "User-level threads are managed by the user-level libraries, not visible to the kernel, whereas Kernel-level threads are managed directly by the operating system. Kernel threads are slower to create and manage than the user threads, but they are more concurrent.")
("What is a preemptive scheduling in an operating system?", "Preemptive scheduling allows an operating system to switch from one task to another while the first task is still executing.")
("What is the goal of the shortest job next (SJN)scheduling algorithm?", "The goal is to favor short jobs. Once the CPU has  finished executing the current process, it selects from the ready queue the job with the smallest estimated CPU burst time to run next—thereby minimizing average waiting and turnaround times.")
("What are semaphores in the context of operating systems?", "Semaphores are variable or abstract data types that are used for controlling access, by multiple processes, to a common resource in a parallel programming or a multi user environment.")
("What is a race condition?", "A race condition occurs when two or more processes can access shared data and they try to change it at the same time. As a result, the values of variables may be unpredictable and vary depending on the timings of context switches of the processes.")
("What is deadlock prevention?", "Deadlock prevention is a set of methods for preventing deadlocks in a system. This can be done by preventing one of the four Coffman conditions from occurring.")
("What is deadlock avoidance?", "Deadlock avoidance requires that the operating system be given in advance additional information concerning which resources a process will request, and when, during its lifetime. With this additional knowledge, it can decide for each request whether the process should wait in order to avoid a possible future deadlock.")
("What is fragmentation in the context of operating systems?", "Fragmentation refers to the condition where memory is wasted because it cannot be used. Fragmentation occurs in a dynamic memory allocation system when many of the free blocks are too small to satisfy any request.")
("What is a page fault?", "A page fault is a type of exception raised by computer hardware when a running program accesses a memory page that is not currently mapped by the memory management unit (MMU)into the virtual address space of a process.")
("What is demand paging?", "Demand paging is a method of virtual memory management where a page is only loaded into memory if an attempt is made to access it and that page is not already in memory (i.e., if a page fault occurs)")
("What is a page replacement algorithm?", "A page replacement algorithm decides which memory pages to page out (swap out, write to disk)when a page of memory needs to be allocated. This happens whenever a page fault occurs for a page that is not already in memory.")
("What is a directory in the context of operating systems?", "In computing, a directory, catalog, or folder, is an entity in a file system which contains a group of files and other directories. A typical file system hierarchy includes at least one directory.")
("What is access control in the context of file systems?", "Access control for a file system determines who can access files and directories, and what they can do with the contents of these files and directories. Access control can be based on permissions, access control lists (ACLs) or other methods.")
("What are the two modes of operation in a modern system?", "Kernel (privileged)and User (non-privileged)")
("What is time-sharing system in an operating system?", "In a time-sharing system, the CPU executes multiple jobs by switching among them, but the switches occur so frequently that the users can interact with each job while it is running.")
("What is a system program?", "System programs provide a convenient environment for program execution. These programs are loaded into memory to become system processes, which then provide services to the user and to other system processes.")
("What is system generation?", "System generation (or sysgen)is the process of creating a particular instance of the system. The operating system is normally distributed on disk or CD-ROM. To install it, you boot the machine with a minimal kernel and then use sysgen to customize the system for your hardware.")
("What is a daemon process?", "A daemon is a computer program that runs as a background process, rather than being under the direct control of an interactive user.")
("What is process scheduling?", "Process scheduling is an activity of the Process Manager that handles the removal of the running process from the CPU and the selection of another process on the basis of a particular strategy.")
("What is a lightweight process?", "Lightweight process (LWP)is a means of achieving multitasking. In the traditional meaning of the term, as used in Unix System V and Solaris, a lightweight process shares its address space and system resources with other lightweight processes within the parent process.")
("What is the difference between concurrent and parallel execution?", "Concurrent execution is when two tasks can start, run, and complete in overlapping time periods. It doesn't necessarily mean they'll ever both be running at the same instant. Parallel execution is when two tasks literally run at the same time, eg. on a multicore or multiprocessor.")
("What is the dispatcher in CPU scheduling?", "The dispatcher is the module that gives control of the CPU to the process selected by the short-term scheduler. This function involves switching context, switching to user mode, and jumping to the proper location in the user program to restart that program.")
("What is convoy effect in CPU scheduling?", "The convoy effect happens under the FCFS scheduling algorithm, where a short process gets stuck behind a long one causing all the other relatively short processes to wait.")
("What is a semaphore?", "A semaphore is a variable or abstract data type used to control access to a common resource by multiple processes in a concurrent system such as a multitasking operating system.")
("What is a critical-section problem?", "The critical-section problem is to design a protocol that the processes can use to cooperate. Each process must request permission to enter its critical section.")
("What are resources in the context of deadlocks?", "In computer science, resources are any quantifiable entities that a job requires for completion. This could include memory, CPU time, disk space, access to I/O devices etc.")
("What is a safe state with regards to deadlock prevention?", "A system is in a safe state if there exists a safe sequence of all processes. A sequence of processes is a safe sequence for the current allocation state if, for each Pi, the resources that Pi can still request can be satisfied by the currently available resources plus the resources held by all Pj, with j < i.")
("What is Swapping in context of memory management?", "Swapping is a mechanism in which a process can be swapped temporarily out of main memory to move into disk or backing storage and then brought back into memory for continued execution.")
("What is Belady's Anomaly?", "Belady's Anomaly is the phenomenon in which increasing the number of page frames results in an increase in the number of page faults for certain memory access patterns. This phenomenon is commonly experienced in some page replacement algorithms, such as the FIFO page replacement algorithm.")
("What is Thrashing?", "Thrashing is a condition in which excessive paging operations are taking place. A system, in a state of thrashing, spends more time paging than executing transactions which leads to a severe degradation of system performance.")
("What is the working-set model?", "The working-set model is based on the principle of locality. It says that, at any given moment, a process only needs a certain set of pages to continue running. This set of pages is known as the working set of the process.")
("What is a File Control Block?", "A File Control Block (FCB)is a computer system structure that contains details about a file such as ownership, permissions, and location of the file, size and date of creation, where data content of a file is stored, and so on.")
("What is a symbolic link?", "A symbolic link, also termed a soft link, is a special kind of file that points to another file, much like a shortcut in Windows or a Macintosh alias. Unlike a hard link, a symbolic link does not contain the data in the target file. It simply points to another entry somewhere in the file system.")
("What is a kernel in an operating system?", "The kernel is the central component of most computer operating systems; it's a bridge between applications and the actual data processing done at the hardware level.")
("What is multiprogramming?", "Multiprogramming is the rapid switching of the CPU between multiple processes in memory. Its goal is to maximize CPU utilization.")
("What is a system call?", "A system call is a programmatic way in which a computer program requests a service from the kernel of the operating system it is executed on.")
("What is the role of the device manager in an operating system?", "The device manager is responsible for controlling all the devices installed on a computer. It performs operations such as allocating devices, deallocating devices, and handling device errors.")
("What is a process state?", "The status of a process at a certain point in time is referred to as a process state. It can be 'new', 'ready', 'running', 'waiting', or 'terminated'.")
("What is process control block?", "A Process Control Block (PCB)is a data structure maintained by the Operating System for every process. The PCB is identified by an integer process ID (PID) A PCB keeps all the information needed to keep track of a process as listed in the text.")
("What is a single-threaded process?", "A single-threaded process has one program counter specifying the location of the next instruction to execute. The process executes instructions sequentially, one at a time, until completion.")
("What is multithreading?", "Multithreading is the ability of a central processing unit (CPU)(or a single core in a multi-core processor)to provide multiple threads of execution concurrently.")
("What is Round Robin (RR)scheduling?", "Round Robin (RR)is one of the simplest, oldest, fairest, and most widely used algorithms. Each process gets a small unit of CPU time (time quantum) usually 10-100 milliseconds.")
("What is Priority Scheduling?", "Priority scheduling is a method of scheduling processes that is based on priority. In this algorithm, the CPU selects from the ready queue the process with the highest priority to run next—ensuring that more important or time-critical tasks are handled sooner.")
("What is Mutual Exclusion?", "Mutual exclusion is a situation where only one process is allowed to execute the critical section (CS)at any given time. It is one of the primary requirement to solve a critical-section problem.")
("What is a lock variable?", "A lock variable is a type of variable used in synchronizing the access of multiple processes to shared data. When a process is using a shared variable it 'locks' it, preventing other processes from using it simultaneously.")
("What is a starvation in the context of deadlocks?", "Starvation is a resource management problem where a process does not get the resources it needs for a long time because the resources are being allocated to other processes.")
("What is a resource-allocation graph?", "A resource-allocation graph is used to depict a system of resources and processes, with each process and each resource represented by a node.")
("What is fragmentation?", "Fragmentation refers to the condition where memory is divided into large blocks of unallocated memory and small blocks of allocated memory. It can be internal or external.")
("What is Paging?", "Paging is a memory management scheme that eliminates the need for contiguous allocation of physical memory. This scheme permits the physical address space of a process to be noncontiguous.")
("What is a Page Table?", "A page table is the data structure used by a virtual memory system in a computer operating system to store the mapping between virtual addresses and physical addresses.")
("What is demand paging?", "Demand paging is a method of managing computer memory. In a system that uses demand paging, the operating system copies a disk page into physical memory only if an attempt is made to access it and that page is not already in memory (i.e., if a page fault occurs)")
("What is a directory in the context of file systems?", "In computing, a directory is a file system cataloging structure which contains references to other computer files, and possibly other directories.")
("What are file attributes?", "File attributes are metadata associated with computer files that define file system behavior. Each attribute can have one of two states: set and cleared. Attributes are set when the file is created, but can be changed thereafter.")
("Consider a system with a clock rate of 2 GHz. If a process spends 20 clock cycles for a particular operation, how much time does it spend for that operation?", "10 nanoseconds")
("Consider three processes, all arriving at time zero, with total execution time of 10, 20 and 30 milliseconds, respectively. Each process spends the first 20% of execution doing I/O, the next 70% of time doing computation, and the last 10% doing I/O again. If operating system uses a shortest remaining time first (SRTF)scheduling algorithm, what's the waiting time for each process?", "Process 1: 0 ms, Process 2: 10 ms, Process 3: 30 ms")
("Consider a counting semaphore S. Can it be initialized to a negative number? If so, what does that indicate? If not, why not?", "No, a semaphore cannot be initialized to a negative number. A semaphore is a synchronization tool used to solve critical section problems and to achieve process synchronization in the multiprocessing environment. It can only be initialized to a non-negative number which represents the number of resources available.")
("Consider a system with 200 tape drives. There are three processes: P1 (max need 10) P2 (max need 98) and P3 (max need 180) Is this system in a safe state?", "No")
("Consider a system with 1 MB of memory, a byte addressable system, and a 32-bit architecture. How many entries are there in a page table if each page is of size 4 KB?", "256")
("A system uses 3-bit page numbers and 2-bit frame numbers. How many pages and frames are there, respectively?", "Pages: 8, Frames: 4")
("If a disk has 4000 blocks and we're using single-level index blocks and each file needs one index block, how many files can we have?", "4000 files, as each file needs one block for the file index and presumably one block for the file content.")
("A system has a 32-bit virtual address space with a page size of 4 kilobytes. How many entries are there in the page table?", "1 million")
("A system has a 64-bit virtual address space and a page size of 8 kilobytes. How many bits are used for the page offset?", "13 bits")
("Consider a system with a 2 GHz clock rate. A process needs 200 clock cycles for a particular operation. What is the time taken for that operation?", "0.1 microseconds")
("In a multi-level feedback queue scheduling algorithm, the time quantum for the higher priority queue is 5 milliseconds, and the quantum for the lower priority queue is 10 milliseconds. A new process P arrives and needs a total time of 30 milliseconds. How much waiting time will P experience?", "20 milliseconds")
("In a system with demand paging, a certain software spends 40% of its time waiting for page faults to be serviced (disk I/O) If a page fault takes an average of 10 milliseconds to service, how much faster would the software run if all its pages were always resident and no page faults occurred?", "2/3 or about 67% faster")
("Consider a disk queue with requests for I/O to blocks on cylinders 10, 22, 20, 2, 40, 6, and 38, in that order. If the C-SCAN scheduling algorithm is used and the head is initially at cylinder 20 moving toward larger cylinder numbers, what is the total head movement in cylinders?", "68 cylinders")
("In a system using 5 state process model (new, ready, running, waiting, terminated) there are currently 10 new processes, 15 ready processes, 2 running processes, 5 waiting processes and 20 terminated processes. How many processes are there in the system?", "52 processes")
("A system uses 3 level paging with page table entry size as 4 bytes. The virtual memory size is 4 GB and page size is 4 KB. What is the size of each page table at each level?", "First Level: 4 KB, Second Level: 4 MB, Third Level: 4 GB")
("Given a system with 2 GB physical memory and 500 MB is occupied by system files. If each user process occupies 50 MB of memory (and the system is designed to have 30 user processes in memory simultaneously) how much memory is still free?", "500 MB")
("What is the command to list all files in a directory in Bash?", "'ls'")
("How would you print 'Hello, World!' in Bash?", "echo 'Hello, World!'")
("In Bash, how would you assign the value 10 to a variable 'a'?", "a=10")
("Write a Bash script to iterate through numbers 1 to 5 and print each number.", "#!/bin/bash\n\nfor i in {1..5}\ndo\n   echo \"$i\"\ndone")
("In Bash, what does the 'cd' command do?", "'cd' stands for 'Change Directory'. It's used to change the current working directory in Linux and other Unix-like operating systems.")
("What does the 'pwd' command do in Bash?", "'pwd' stands for 'Print Working Directory'. It prints the path of the current working directory.")
("What does the '>' operator do in Bash?", "The '>' operator in Bash is used for redirection. It redirects the output of a command to a file, overwriting the existing contents of the file.")
("What does the '>>' operator do in Bash?", "The '>>' operator in Bash is used for redirection. It redirects the output of a command to a file, appending it to the existing contents of the file.")
("What does the '|' operator do in Bash?", "The '|' operator in Bash is called a pipe. It's used to pass the output of one command as input to another command.")
("How do you get the number of lines in a file in Bash?", "You can use the 'wc -l' command followed by the file name to get the number of lines in a file. 'wc' stands for 'word count', and the '-l' option tells it to count lines.")
("What is the command to connect to a remote server using SSH?", "'ssh user@hostname' where 'user' is the username and 'hostname' is the name or IP address of the remote server.")
("How do you open a file for editing using the Nano editor in Bash?", "You can use 'nano filename', where 'filename' is the name of the file you want to edit.")
("In Bash, what does the 'fg' command do?", "'fg' stands for 'foreground'. It continues a stopped job by running it in the foreground.")
("What does the '&' operator do in Bash?", "In Bash, the '&' operator is used to put a process in the background.")
("How do you compile a C program named 'program.c' in Bash?", "You can use 'gcc program.c -o program' to compile a C program. 'gcc' is the GNU C Compiler, 'program.c' is the source file, and '-o program' specifies the output file.")
("How do you compile a C++ program named 'program.cpp' in Bash?", "You can use 'g++ program.cpp -o program' to compile a C++ program. 'g++' is the GNU C++ Compiler, 'program.cpp' is the source file, and '-o program' specifies the output file.")
("In Bash, how do you run an executable file named 'program'?", "You can use './program' to run an executable file in the current directory.")
("How can you use SSH to run a command on a remote server?", "You can use 'ssh user@hostname command', where 'user' is the username, 'hostname' is the name or IP address of the remote server, and 'command' is the command you want to run.")
("In Bash, how do you save and close a file when using Nano editor?", "You can use 'Ctrl+X' to exit, and you will be prompted to save changes. Press 'Y' to save and enter to confirm the file name.")
("What is the purpose of the 'make' command in compiling C or C++ programs?", "'make' is a utility that automatically builds executable programs and libraries from source code. It reads files called Makefiles which specify how to derive the target program.")